# Performance scenario notes

Use `npm run mock:scenario -- large`, `slow`, or `race-condition` before measuring the unchanged frontend. Keep fixed seeds and report measurements first. Do not turn performance observations into application changes on the initial QA pass.
